{
  "name": "be2-mcp-lint-toolchain",
  "private": true,
  "version": "0.0.0",
  "description": "Isolated async lint toolchain (eslint + typescript-eslint) with its own typescript 5.9 — kept separate from root typescript@7 to avoid ERESOLVE on fresh install.",
  "devDependencies": {
    "eslint": "^10.9.1",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.69.0"
  }
}
