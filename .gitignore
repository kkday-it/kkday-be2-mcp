# Secrets — NEVER commit
.env
.env.*
!.env.example

# Node / TS (be2-mcp server)
node_modules/
dist/
build/
*.tsbuildinfo

# Logs / OS
*.log
.DS_Store

# Playwright / scratch
.playwright-mcp/
.superpowers/

# Data and test fixtures
data/
tests/fixtures/*.local.json

# 對外提案 / 概覽草稿 — 刻意不進 git
docs/be2-mcp/cto-proposal.md
docs/be2-mcp/poc-overview.md

# test-generated cassette fixtures (recreated at runtime)
tests/support/__fixtures__/
